# Committed test configuration: used by integration tests, HTTP API tests, and Playwright.
# Points at the isolated shopops_test database so the dev database is never touched.

DATABASE_URL="postgresql://shopops:shopops_local_dev@localhost:5432/shopops_test"
APP_URL="http://localhost:3100"
PORT=3100
